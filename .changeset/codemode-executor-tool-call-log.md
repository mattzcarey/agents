---
"@cloudflare/codemode": patch
---

Add ordered `toolCalls` metadata to codemode executor/code-tool results so frontends can display the sandbox tool calls, arguments, results, and errors made during an execution.
